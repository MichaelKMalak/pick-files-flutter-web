name: webfilepicker
description: A toy app for testing file picker

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker_web:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true